# 2026-07-06 Day 038 Log

## 오늘의 목표

크라베오 AI 최적화 — q4_0 변환 + 컨텍스트 축소. 어제(Day 037) 발견한 CPU 100%, 메모리 5.3GB 문제 해결.

## 실행한 작업

- Colab에서 `Day038_Convert_q4.ipynb` 실행 — HuggingFace LoRA 가중치 로드 → q4_0 GGUF 변환
- `Modelfile-v3` 작성 (q4_0 + num_ctx 2048) 후 `ollama create cravveo:v3` 등록
- AnythingLLM에서 "2026-06-27일에 뭐했어?" 등 날짜 기반 질문 테스트 중 RAG 완전 실패 발견
- AnythingLLM 내부 DB(`anythingllm.db`), LanceDB 벡터 저장소 직접 확인하며 원인 추적

## 확인한 결과 (버그 3개 순서대로 발견 및 수정)

1. **채팅 모드가 "에이전트"로 설정됨** → `llama3.2:3b` 같은 소형 모델이 도구 호출(tool call)을 못 해서 검색 자체가 실행 안 됨 (`sources: []`, JSON 도구 호출문이 텍스트로 그대로 출력되는 것 확인). "채팅" 모드로 변경 → 해결
2. **Search Preference가 "Default"** → 속도 우선이라 관련성 낮은 문서를 가져와 환각 유발 (AnythingLLM 자체 설명에도 명시됨). "Accuracy Optimized"로 변경
3. **최대 문맥 조각을 100으로 과도하게 설정** → 모델 컨텍스트 용량 초과로 정작 정답 문서가 잘려나감. 6~8 수준으로 재조정 → "2026-07-03에 뭐했어?" 질문에 정확한 출처(`2026-07-03_Day037_Log.md`) 1순위로 검색 성공

## 막힌 지점

날짜 기반 질문("2026-06-27일에 뭐했어?")에 임베딩 검색이 구조가 비슷한 다른 Day 로그를 잘못 가져오는 문제. 유사성 임계값/Search Preference 조정으로 개선했으나, 완전한 해결책은 아님 — 문서 구조가 거의 동일해서 발생하는 임베딩 검색의 근본적 한계.

## 해결 방법

- 채팅 모드: 에이전트 → 채팅
- Search Preference: Default → Accuracy Optimized
- 문서 유사성 임계값: 낮음(.25) → 제한 없음
- 최대 문맥 조각: 100 → 적정 수준(6~8)

## 성공 조건

- [x] q4_0 GGUF 변환 완료
- [x] Ollama에 v3 모델 등록
- [ ] 메모리 사용량 3GB 이하 확인 (RAG 디버깅으로 시간 소요, 미측정)
- [x] AnythingLLM RAG 정상 작동 확인 (설정 3개 버그 수정)
- [ ] 영상 녹화
- [x] 커밋 + 푸시

## 오늘의 핵심 발견

RAG가 안 되는 이유를 겉으로 보이는 증상(모델을 바꿔도 똑같음)만으로 판단하지 않고, AnythingLLM의 SQLite DB와 LanceDB 벡터 저장소를 직접 열어서 `sources: []`라는 실제 데이터를 확인한 게 결정적이었다. 문제는 모델이 아니라 워크스페이스 설정(채팅 모드, 검색 방식, 문맥 조각 수) 3곳에 나뉘어 있었고, 하나씩 고칠 때마다 증상이 바뀌는 걸 보면서 순차적으로 원인을 좁혀갈 수 있었다. "실행되면 OK"가 아니라 "왜 안 되는지 끝까지 파는" 방식이 실제로 효과가 있었다.

## 다음 단계

Day 039 — 환각 문제 분석 + RAG 도입 준비. 특히 임베딩 검색이 비슷한 구조의 문서를 구분 못 하는 근본 문제(날짜 매칭 실패)를 어떻게 완화할지 (메타데이터 필터링, 청킹 전략 개선 등) 검토.

## 요약 (유튜브 고정 댓글용)

```text
Day 038 완료 | Day 038 Complete

q4_0 변환으로 모델 경량화 완료. 그리고 AnythingLLM RAG가 안 되던 버그 3개를 순서대로 찾아 고쳤습니다.
Finished q4_0 model optimization. Also found and fixed 3 stacked bugs breaking AnythingLLM's RAG.

오늘의 핵심 발견 | Key discovery:
"모델을 바꿔도 안 된다"는 증상만 보고 판단하지 않고, 앱 내부 DB를 직접 열어봐야 진짜 원인이 보였습니다.
Instead of judging by symptoms alone ("still broken after switching models"), opening the app's internal DB revealed the real cause.

다음 단계 | Next step:
Day 039 — 환각 문제 심화 분석 + RAG 개선

GitHub: https://github.com/cravveo4001/cravveo-100days
Model: https://huggingface.co/cravveo/cravveo-llama-lora
```

---

[[2026-07-03_Day037_Log|← Day 037]] | [[../38일차/2026-07-04_Day038_Work_Order|Day 038 작업지시서]]
