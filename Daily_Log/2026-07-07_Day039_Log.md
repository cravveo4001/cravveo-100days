# 2026-07-07 Day 039 Log

## 오늘의 목표

원래 계획: 임베딩 검색이 구조가 비슷한 Day 로그를 정확히 구분 못 하는 문제(Day 038 발견)를 체계적으로 테스트하고, 문서 보강으로 개선되는지 실험.

## 실행한 작업

- `cravveo:v3`로 날짜 기반 질문 10개 체계적 테스트 시작
- 테스트 중, RAG 매칭 여부와 무관하게 `cravveo:v3`가 문맥과 상관없는 헛소리(예: "크래비보 Company 3번째 AI")를 생성하는 문제 발견 → **방향 전환**
- `cravveo:latest`(v2, q8_0)와 A/B 비교 → v2는 느리지만 말이 되는 답변, 원인이 q4_0 양자화로 좁혀짐
- Colab에서 `Day039_Convert_q4km.ipynb` 작성 및 실행 → LoRA 가중치를 `q4_K_M`(K-quant)으로 재변환
  ([Colab 노트북](https://colab.research.google.com/drive/1tkvdG80gHwN40VcbbAHk73-MEnebc1vO?usp=sharing))
- `Modelfile-v4`로 `cravveo:v4` 등록, 답변이 200토큰에서 잘리는 문제 발견 → `num_predict` 200→500으로 수정 후 재등록
- v4로 방향 전환 전 계획(문서 보강 실험) 재개: `2026-05-23_Day001_Log.md`, `2026-05-27_Day004_Log.md`에 날짜↔Day 매핑 문장 추가 후 재업로드/재임베딩

## 확인한 결과

1. **q4_0 → q4_K_M 전환으로 모델 품질 문제 해결.** v3는 문맥과 무관한 헛소리, v4는 v2 수준으로 말이 되는 답변 생성 확인.
2. **문서 보강 실험은 실패.** AnythingLLM 내부 DB로 재임베딩이 정상적으로 됐음을 직접 확인했음에도(새 벡터 청크 생성, 추가한 문장이 본문에 포함됨), 여전히 정답 문서가 검색되지 않음.

## 막힌 지점

문서 안에 "이 기록은 2026-05-23, Day 001에 해당합니다"라는 문장을 명시적으로 추가해도 검색 결과가 바뀌지 않음. 37개 문서가 구조적으로 거의 동일해서, 짧은 날짜 문자열 하나가 문서 전체의 임베딩 벡터를 충분히 바꾸지 못하는 것으로 보임.

## 해결 방법

의미 기반 벡터 검색으로는 정확한 날짜 매칭이 구조적으로 어렵다고 결론. RAG 설정/문서 보강으로는 더 이상 개선 여지가 없다고 판단해 방향을 바꾸기로 함 — **날짜→Day 번호를 코드(파이썬 딕셔너리)로 직접 매핑하는 룩업 테이블** 방식으로 전환. AI 검색에 맡기지 않고 결정론적으로 100% 정확하게 처리. 구현은 Day 040으로 이월.

## 성공 조건

- [x] q4_0 헛소리 문제의 원인이 양자화 방식임을 A/B 비교로 확인
- [x] q4_K_M으로 재변환 + cravveo:v4 등록, 품질 정상 확인
- [x] 문서 보강 실험 시도 → 실패, RAG의 구조적 한계로 결론
- [ ] 룩업 테이블 구현 (Day 040으로 이월)
- [ ] 영상 녹화
- [x] 커밋 + 푸시

## 오늘의 핵심 발견

계획대로 진행되지 않을 때 두 번 방향을 틀었다. 첫 번째는 RAG 테스트 중 모델 자체가 헛소리하는 걸 발견하고 더 급한 문제로 전환한 것, 두 번째는 문서 보강 실험이 실패한 걸 보고 "설정을 더 만지는 것"과 "다른 도구로 바꾸는 것"을 구분한 것. 임베딩 검색을 아무리 조정해도 안 되는 건 그 기술 자체의 한계이지, 더 시도해서 풀릴 문제가 아니었다. 안 되는 걸 계속 붙잡는 대신, 원인을 명확히 확인한 뒤 다른 방법(결정론적 룩업 테이블)으로 바꾸기로 결정한 게 오늘의 진짜 성과.

## 다음 단계

Day 040 — 날짜→Day 번호 룩업 테이블 구현. 질문에서 날짜를 감지하면 Day 번호로 미리 치환한 뒤 검색하는 전처리 로직 추가.

---

[[2026-07-06_Day038_Log|← Day 038]] | [[../39일차/2026-07-07_Day039_Work_Order|Day 039 작업지시서]]
